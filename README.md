# Zanac — WebGL production build (GameBull)

- **Play:** https://alishehroz-ideo.github.io/zanac/
- Unity 6000.4.3f1 · WebGL · IdeofuzionBridge template · Brotli (`.unityweb`)
- API: production (`https://api.g-b.store`), overridden at runtime by `?apiBase=`
- Loading icon: `customization.gameLogoUrl` from `/context`, static icon as fallback

Deploy-artifact repo. Source lives in the Unity project, not here.
